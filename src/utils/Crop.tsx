import Cropper, { ReactCropperElement } from "react-cropper";
import "cropperjs/dist/cropper.css";
import {RefObject} from "react";

export default function Crop(props: {innerRef: RefObject<ReactCropperElement>}) {
    // todo
    return (<div></div>)
}
